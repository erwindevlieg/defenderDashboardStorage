# Development-only dependencies (linting, testing, doc coverage).
# Runtime deps live in requirements.txt. Install with:
#   pip install -r requirements-dev.txt
-r requirements.txt

pre-commit>=4.6.0,<5.0
ruff>=0.8,<1.0
mypy>=1.13,<2.0
pytest>=9.0.3,<10.0
pytest-asyncio>=1.0,<2.0
pytest-cov>=5.0,<7.0
aioresponses>=0.7,<1.0
interrogate>=1.7,<2.0
pip-audit>=2.7,<3.0
